{
  "name": "full-stack-template",
  "version": "1.0.0",
  "description": "full stack web app using DERN stack (database is not based only on mongodb)",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "server": "concurrently \"npm run dev --prefix ./server\" \"npm run bundle --prefix ./server\"",
    "client": "npm run dev --prefix ./client"
  },
  "author": "Jason Dirany"
}
